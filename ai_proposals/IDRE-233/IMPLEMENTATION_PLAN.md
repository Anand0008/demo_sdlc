## IDRE-233: Unable to create an invoice for both parties (Initiating and Non-Initiating)

**Jira Ticket:** [IDRE-233](https://orchidsoftware.atlassian.net//browse/IDRE-233)

## Summary
Update the invoice creation forms to remove frontend restrictions that prevent users from selecting and grouping cases from both Initiating and Non-Initiating party types into a single invoice.

## Implementation Plan

**Step 1: Allow mixed party types in Create Invoice Form**  
Locate the validation and selection logic for cases. Remove any checks or filters that prevent a user from selecting both Initiating and Non-Initiating cases simultaneously. Ensure that when the form is submitted, the payload to `createInvoiceFromCases` includes all selected case IDs regardless of their party type.
Files: `app/app/invoices/components/create-invoice-form.tsx`

**Step 2: Allow mixed party types in Invoice Payment Option**  
Review the `selectedCases` validation logic prior to invoice generation. Remove any restrictions that enforce all selected cases to share the same `partyType`. Ensure the UI permits the selection of cases across both party types and successfully passes them to the invoice creation action.
Files: `app/app/payments/components/invoice-payment-option.tsx`

**Risk Level:** MEDIUM — The payments and refunds engine is a complexity hotspot. Allowing mixed party types in a single invoice could potentially affect downstream payment allocations or status transitions if the system implicitly relied on invoices being segregated by party type.

**Deployment Notes:**
- Ensure QA tests invoice creation with a mix of Initiating and Non-Initiating cases.
- Verify that payment status transitions remain accurate when an invoice contains cases from both party types.

## Proposed Code Changes

### `app/app/invoices/components/create-invoice-form.tsx` (modify)
Removed the validation logic that restricts users from selecting cases with different party types (Initiating and Non-Initiating) simultaneously when creating an invoice.
```
--- a/app/app/invoices/components/create-invoice-form.tsx
+++ b/app/app/invoices/components/create-invoice-form.tsx
@@ -...@@
-    const selectedPartyTypes = new Set(
-      cases.filter((c) => selectedCases.includes(c.id)).map((c) => c.partyType)
-    );
-    if (selectedPartyTypes.size > 1) {
-      setError("Cannot mix Initiating and Non-Initiating cases in a single invoice.");
-      return;
-    }
```

### `app/app/payments/components/invoice-payment-option.tsx` (modify)
No rationale provided
```
Type: modify

```diff
--- a/app/app/payments/components/invoice-payment-option.tsx
+++ b/app/app/payments/components/invoice-payment-option.tsx
@@ -...@@
-    const partyTypes = new Set(selectedCases.map((c) => c.partyType));
```

## Test Suggestions

Framework: `Vitest / React Testing Library`

- **should allow selecting both Initiating and Non-Initiating cases without validation errors** — Verifies that the frontend restriction preventing the selection of mixed party types has been removed.
- **should submit successfully when cases from both party types are selected** — Ensures that the form successfully submits a mixed-party invoice payload.
- **should render payment options correctly for an invoice with mixed party types** *(edge case)* — Verifies that the payment options component can handle and correctly display an invoice containing mixed party types.

## Confluence Documentation References

- [IDRE Worflow](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/284688394) — Defines the case lifecycle, including the roles of the Initiating Party (IP) and Non-Initiating Party (NIP), and the Payment/Accounting actor.
- [Bugs](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/285736962) — Identifies the payments and refunds logic as a major complexity hotspot, explicitly mentioning "invoice" as a payment method and testing scenarios where "Both parties paid vs only one paid".
- [IDRE Case Workflow Documentation](https://orchidsoftware.atlassian.net/wiki/spaces/SD/pages/229277697) — Defines the IDRE case workflow phases, including "Payment Collection", and the key parties (IP and NIP) involved in the dispute.

**Suggested Documentation Updates:**

- Bugs - May need updating to reflect the resolution of the invoice creation issue and any new QA test matrices for dual-party invoicing.
- IDRE Case Workflow Documentation - May need updating if the payment collection workflow or invoice generation process changes for IP and NIP.

## AI Confidence Scores
Plan: 80%, Code: 85%, Tests: 90%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._