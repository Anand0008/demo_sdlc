## IDRE-270: While uploading new case when user selects create new organization option and provides existing organziation name should be able to see error

**Jira Ticket:** [IDRE-270](https://orchidsoftware.atlassian.net//browse/IDRE-270)

## Summary
Add client-side validation to prevent users from creating a new organization with a name that already exists during the case upload/review process.

## Implementation Plan

**Step 1: Add duplicate name validation to OrganizationEditDialog**  
In the `handleCreateOrganization` function, add a validation check before setting `creatingOrg` to true. Check if `newOrgName.trim().toLowerCase()` matches any `org.name.toLowerCase()` in the `availableOrganizations` array. If a match is found, call `toast.error("An organization with this name already exists.")` and return early to prevent creation.
Files: `components/case-review/organization-edit-dialog.tsx`

**Step 2: Add duplicate name validation to NipOrganizationStep**  
Locate the function responsible for handling new organization creation. Add a similar validation check to compare the new organization name against the `availableOrganizationsRaw` array (case-insensitive). If a duplicate is found, display an error using `toast.error` and abort the creation process.
Files: `components/case-review/party-wizard-step-organization.tsx`

**Risk Level:** LOW — The changes are purely client-side validation additions using existing data arrays and error notification patterns. They do not alter backend logic or database schemas.

## Proposed Code Changes

### `components/case-review/organization-edit-dialog.tsx` (modify)
Prevents the creation of duplicate organizations by checking the new organization name against the list of available organizations before submitting the creation request.
```
--- a/components/case-review/organization-edit-dialog.tsx
+++ b/components/case-review/organization-edit-dialog.tsx
@@ -53,6 +53,14 @@
   const handleCreateOrganization = async (e: React.FormEvent) => {
     e.preventDefault();
     if (!newOrgName.trim()) return;
 
+    const isDuplicate = availableOrganizations.some(
+      (org) => org.name.toLowerCase() === newOrgName.trim().toLowerCase()
+    );
+    if (isDuplicate) {
+      toast.error("An organization with this name already exists.");
+      return;
+    }
+
     setCreatingOrg(true);
     try {
```

### `components/case-review/party-wizard-step-organization.tsx` (modify)
Adds the same duplicate name validation to the NIP organization step, ensuring consistency across the case review process.
```
--- a/components/case-review/party-wizard-step-organization.tsx
+++ b/components/case-review/party-wizard-step-organization.tsx
@@ -150,6 +150,14 @@
   const handleCreateOrganization = async () => {
     if (!newOrgName.trim()) return;
 
+    const isDuplicate = availableOrganizationsRaw.some(
+      (org) => org.name.toLowerCase() === newOrgName.trim().toLowerCase()
+    );
+    if (isDuplicate) {
+      toast.error("An organization with this name already exists.");
+      return;
+    }
+
```

**New Dependencies:**
- `None`

## Test Suggestions

Framework: `Vitest / React Testing Library`

- **should display error and prevent submission when new organization name already exists** *(edge case)* — Verifies that the client-side validation catches duplicate organization names and blocks the creation request, reproducing the fix for IDRE-270.
- **should allow submission when new organization name is unique** — Verifies that valid, unique organization names can be submitted without validation errors (Happy Path).
- **should show validation error when entering an existing organization name in the wizard step** *(edge case)* — Ensures the NIP organization wizard step also prevents duplicate organization names, maintaining consistency across the case review process.

## Confluence Documentation References

- [Pre-Staging Readiness and End-to-End Testing Workflow for Development and QA Team](https://orchidsoftware.atlassian.net/wiki/spaces/SD/pages/296910852) — This page outlines the QA workflow for Case Initiation & Upload, specifically mentioning the Organization Selection step where users can create a new organization. The ticket adds a new validation rule to this exact flow.

**Suggested Documentation Updates:**

- Pre-Staging Readiness and End-to-End Testing Workflow for Development and QA Team: Needs to be updated to include a test case for verifying the error message when attempting to create a new organization with an existing name during case upload.

## AI Confidence Scores
Plan: 95%, Code: 95%, Tests: 95%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._