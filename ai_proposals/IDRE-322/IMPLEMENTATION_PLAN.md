## IDRE-322: Report: IDRE Payouts

**Jira Ticket:** [IDRE-322](https://orchidsoftware.atlassian.net//browse/IDRE-322)

## Summary
Create a standalone SQL script to manually generate the IDRE Payouts report for Halo, VeraTru, and Capitol Bridge, as an in-system report is not yet required.

## Implementation Plan

**Step 1: Create manual SQL report script**  
Create a new SQL script file containing the query to extract the IDRE payouts report. The query will SELECT the Payee (coalescing `bankingSnapshot.accountHolderName` and `recipientName`), Dispute ID (`c.disputeReferenceNumber`), and Date Payment Completed (formatting the earliest available completion timestamp from `payment`). It will JOIN `payment`, `case_payment_allocation`, and `case` tables, and filter for `direction = 'OUTGOING'`, `status = 'COMPLETED'`, and payees matching 'halo', 'veratru', or 'capitol bridge'.
Files: `scripts/idre-payouts-report.sql`

**Risk Level:** LOW — The change only adds a standalone SQL script for manual execution. It does not modify any application code, API endpoints, or database schemas, resulting in zero risk to the running application.

**Deployment Notes:**
- The SQL script does not need to be deployed as part of the application build, but should be merged into the repository for version control and future use.

## Proposed Code Changes

### `scripts/idre-payouts-report.sql` (create)
The ticket requests a manual report for IDRE payouts to date for specific payees. An in-system report is not yet required, so a standalone SQL script is provided to extract the necessary data (Payee, Dispute ID, Date Payment Completed).
```sql
JSON_UNQUOTE(JSON_EXTRACT(p.bankingSnapshot, '$.accountHolderName')),
p.recipientName
```

**New Dependencies:**
- `None`

## Test Suggestions

Framework: `Jest or Vitest (Integration)`

- **shouldReturnCorrectColumnsAndDataForTargetPayees** — Verifies that the SQL script successfully extracts the required columns for the specified payees.
- **shouldExcludePayoutsFromOtherPayees** *(edge case)* — Ensures the SQL script correctly filters the results to only include the three requested payees.
- **shouldExcludeIncompleteOrPendingPayments** *(edge case)* — Verifies that the report only pulls payouts that have actually been completed to date.

## Confluence Documentation References

- [Case Balance Report](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/308936719) — Provides the exact database schema and table relationships (`case`, `case_payment_allocation`, `payment`) needed to query dispute IDs and payment dates for the report.
- [IDRE Worflow](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/284688394) — Provides business context on Capitol Bridge (one of the required payees) and confirms the format and terminology for the Dispute ID (`disputeReferenceNumber`).

**Suggested Documentation Updates:**

- IDRE Reports Bot (May need to be updated to support natural language queries for IDRE payouts once the SQL logic is established)
- A new Confluence page should likely be created to document the SQL query for the "IDRE Payouts Report" for future manual pulls

## AI Confidence Scores
Plan: 95%, Code: 100%, Tests: 85%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._