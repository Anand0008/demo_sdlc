## IDRE-471: Application Error: a client-side exception has occurred.

**Jira Ticket:** [IDRE-471](https://orchidsoftware.atlassian.net//browse/IDRE-471)

## Summary
Fix client-side exception by adding Suspense boundaries around components using useSearchParams.

## Implementation Plan

**Step 1: Add Suspense boundary to Banking Page**  
Import `Suspense` from `react` and `BankingLoading` from `./loading`. Wrap the `<BankingClient>` component in a `<Suspense>` boundary with `fallback={<BankingLoading />}` to prevent client-side exceptions caused by `useSearchParams`.
Files: `app/dashboard/banking/page.tsx`

**Step 2: Add Suspense boundary to Payments Page**  
Import `Suspense` from `react` and `PaymentsLoading` from `./loading`. Wrap the `<PaymentsClient>` component in a `<Suspense>` boundary with `fallback={<PaymentsLoading />}` to prevent client-side exceptions caused by `useSearchParams`.
Files: `app/dashboard/payments/page.tsx`

**Risk Level:** LOW — Adding Suspense boundaries is a standard Next.js practice for components using `useSearchParams` and carries minimal risk. It resolves the client-side exception without altering business logic.

**Deployment Notes:**
- Ensure that the Suspense fallbacks provide a smooth user experience and do not cause layout shift.

## Proposed Code Changes

### `app/dashboard/banking/page.tsx` (modify)
Next.js requires components using `useSearchParams` to be wrapped in a Suspense boundary. Without it, the entire page can de-opt to client-side rendering and throw a client-side exception during the build or runtime.
```
--- app/dashboard/banking/page.tsx
+++ app/dashboard/banking/page.tsx
@@ -1,2 +1,4 @@
+import { Suspense } from "react";
+import BankingLoading from "./loading";
 
@@ -10,3 +12,5 @@
-    <BankingClient />
+    <Suspense fallback={<BankingLoading />}>
+      <BankingClient />
+    </Suspense>
```

### `app/dashboard/payments/page.tsx` (modify)
Similar to the banking page, the payments client component uses `useSearchParams` and must be wrapped in a Suspense boundary to prevent client-side exceptions.
```
--- app/dashboard/payments/page.tsx
+++ app/dashboard/payments/page.tsx
@@ -1,2 +1,4 @@
+import { Suspense } from "react";
+import PaymentsLoading from "./loading";
 
@@ -10,3 +12,5 @@
-    <PaymentsClient />
+    <Suspense fallback={<PaymentsLoading />}>
+      <PaymentsClient />
+    </Suspense>
```

## Test Suggestions

Framework: `Jest/Vitest with React Testing Library`

- **should render BankingPage without client-side exceptions** — Verifies that the Banking page renders without crashing now that the Suspense boundary is in place.
- **should render PaymentsPage without client-side exceptions** — Verifies that the Payments page renders without crashing now that the Suspense boundary is in place.

## AI Confidence Scores
Plan: 95%, Code: 95%, Tests: 90%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._