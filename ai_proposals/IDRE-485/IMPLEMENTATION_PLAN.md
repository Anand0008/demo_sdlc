## IDRE-485: Add Confirmation Box for traditional closure flows

**Jira Ticket:** [IDRE-485](https://orchidsoftware.atlassian.net//browse/IDRE-485)

## Summary
Implement mandatory confirmation modals for case rendering and defaulting actions in the admin actions tab and case detail page, providing dynamic messaging about prevailing parties and refund implications.

## Implementation Plan

**Step 1: Implement Confirmation Modal in Admin Actions Tab**  
Add a state-driven Dialog component within `AdminActionsTab` to serve as the confirmation modal for 'Default' and 'Render' actions. Add state variables for `isClosureModalOpen`, `closureActionType` (Default, Render, Split), `disputeType` (Single, Bundled, Batched), and `selectedPartyName`. Implement dynamic messaging based on the ticket's acceptance criteria (e.g., injecting the party name and specifying the refund impact). Intercept the existing 'Default' and 'Render' button click handlers to open this modal instead of immediately executing the action. Wire the modal's 'Proceed' button to the original action execution logic, and 'Cancel' to close the modal.
Files: `components/admin/admin-actions-tab.tsx`

**Step 2: Implement Confirmation Modal in Case Detail Page**  
Add a state-driven Dialog component within `CaseDetailPage` to serve as the confirmation modal for 'Render' actions triggered from the main case view. Add state variables for `isRenderModalOpen`, `disputeType`, `isSplitDecision`, and `selectedPartyName`. Implement the dynamic messaging for Single/Bundled, Batched, and Split decisions as specified in the ticket. Intercept the 'Render' button click to open the modal. Wire the 'Proceed' button to finalize the decision and trigger the refund, and 'Cancel' to close the modal.
Files: `app/app/cases/[caseId]/page.tsx`

**Risk Level:** LOW — The changes are purely frontend UI additions (intercepting existing actions with a confirmation modal). No backend logic or database schema changes are required, minimizing risk.

**Deployment Notes:**
- Ensure that the dynamic party names and IP/NIP designations are correctly passed to the modal state before opening.

## Proposed Code Changes

### `components/admin/closure-confirmation-modal.tsx` (create)
No rationale provided
```
message = `You have chosen to render this dispute as Default - "${partyName}". By completing this action, you are defaulting this dispute and "${partyName}"
```

## Test Suggestions

Framework: `Vitest + React Testing Library`

- **should display correct messaging for single and bundled disputes** — Verifies dynamic messaging for Single and Bundled Disputes
- **should display correct messaging for batched disputes** — Verifies dynamic messaging for Batched Disputes
- **should display correct messaging for batched disputes with split decision** — Verifies dynamic messaging for Batched Disputes with a Split Decision
- **should display correct messaging for default actions** — Verifies dynamic messaging for Default actions
- **should call onConfirm when proceed button is clicked** — Verifies that the proceed action triggers the confirmation callback
- **should call onCancel when cancel button is clicked** — Verifies that the cancel action triggers the cancel callback
- **should handle missing partyName gracefully** *(edge case)* — Verifies component behavior when partyName is missing

## Confluence Documentation References

- [IDRE Case Workflow Documentation](https://orchidsoftware.atlassian.net/wiki/spaces/SD/pages/229277697) — Defines the core domain concepts (IP, NIP, Arbitration phase) that are directly referenced in the ticket's dynamic messaging and closure flow requirements.

**Suggested Documentation Updates:**

- IDRE Case Workflow Documentation: Needs to be updated to include the new mandatory confirmation steps, refund transparency messaging, and the specific actions required to finalize a decision or default a case during the Arbitration phase.

## AI Confidence Scores
Plan: 90%, Code: 85%, Tests: 95%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._