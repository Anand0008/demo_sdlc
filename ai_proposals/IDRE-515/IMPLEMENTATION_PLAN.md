## IDRE-515: Appeals specialist unable to mark ineligible 

**Jira Ticket:** [IDRE-515](https://orchidsoftware.atlassian.net//browse/IDRE-515)

## Summary
Update the RBAC configuration to grant the Appeals specialist role the permission to mark cases as ineligible.

## Implementation Plan

**Step 1: Grant 'ineligible' permission to Appeals specialist**  
Locate the `ROLE_DEFINITIONS` object. Find the entry corresponding to the Appeals specialist role (e.g., `appeals-specialist`). Update its `administrative_closure` array to include the `"ineligible"` permission. If the `administrative_closure` key does not exist for this role, add it with `["ineligible"]`.
Files: `lib/auth/permissions.ts`

**Risk Level:** LOW — The change is isolated to the RBAC configuration for a single role. It does not modify the underlying state machine or payment/refund logic, ensuring downstream systems remain unaffected.

**Deployment Notes:**
- Ensure that any cached sessions or permissions for existing Appeals specialists are refreshed or invalidated upon deployment so the new permission takes effect.

## Proposed Code Changes

### `lib/auth/permissions.ts` (modify)
The Appeals specialist role needs the `ineligible` permission under `administrative_closure` to be able to mark cases as ineligible. If the `administrative_closure` array already exists for this role, `"ineligible"` should simply be appended to it.
```typescript
--- a/lib/auth/permissions.ts
+++ b/lib/auth/permissions.ts
@@ -... @@
     "appeals-specialist": {
       cases: [
         "view_all",
         "master_search",
       ],
       arbitration_dashboard: [
         "view",
         "update_assigned",
         "decide_prevailing_party",
       ],
+      administrative_closure: [
+        "ineligible"
+      ],
```

**New Dependencies:**
- `(none)`

## Test Suggestions

Framework: `Jest`

- **should grant Appeals specialist the ineligible permission under administrative_closure** — Verifies that the Appeals specialist role includes the 'ineligible' action within the 'administrative_closure' permissions array, fixing the bug where they could not mark cases as ineligible.
- **should not inadvertently grant ineligible permission to roles without prior access** — Regression test to ensure that adding the 'ineligible' permission to the Appeals specialist did not accidentally grant it to other unauthorized roles.

## Confluence Documentation References

- [Bugs](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/285736962) — Identifies 'Ineligible' as a specific case closure type and highlights its complex interactions with the payments and refunds engine, which must be considered when fixing status transitions.
- [IDRE Case Workflow Documentation](https://orchidsoftware.atlassian.net/wiki/spaces/SD/pages/229277697) — Defines the high-level phases of an IDRE case, confirming that 'Eligibility' is a core phase of the workflow where the 'mark ineligible' action occurs.
- [IDRE Worflow](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/284688394) — Provides the canonical reference for the case lifecycle, including the eligibility review phase and the actors (roles) involved in the process.

**Suggested Documentation Updates:**

- IDRE Worflow
- IDRE Case Workflow Documentation

## AI Confidence Scores
Plan: 95%, Code: 95%, Tests: 95%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._