## IDRE-524: Bug: When user uploads a case of type single should be allowed add single line item

**Jira Ticket:** [IDRE-524](https://orchidsoftware.atlassian.net//browse/IDRE-524)

## Summary
Restrict the case creation wizard to only allow a single line item when the case type is 'single', gated by the IDRE-524 feature flag.

## Implementation Plan

**Step 1: Restrict adding multiple line items for SINGLE case types**  
Locate the section where line items are added (likely using `useFieldArray` or similar). Add a conditional check using the `idre524Enabled` feature flag and the current `typeOfDispute` value. If the feature flag is enabled and `typeOfDispute` is 'SINGLE', disable or hide the 'Add Line Item' button when `lineItems.length >= 1`.
Files: `components/case-wizard/step-basic-info.tsx`

**Step 2: Enforce single line item on case type switch**  
Ensure that if the user switches the case type from 'BATCHED' to 'SINGLE', any existing line items beyond the first one are removed. This logic is already partially present in `handleConfirmSwitchToSingle`, but ensure it is properly hooked up to the UI and respects the `idre524Enabled` feature flag.
Files: `components/case-wizard/step-basic-info.tsx`

**Risk Level:** LOW — The change is isolated to the UI validation logic in the case creation wizard and is gated by a feature flag, minimizing the risk of unintended side effects.

**Deployment Notes:**
- Ensure the IDRE-524 feature flag is enabled in the target environment to activate the fix.

## Proposed Code Changes

### `components/case-wizard/step-basic-info.tsx` (modify)
Added a `useEffect` hook to continuously monitor the `lineItems` and `typeOfDispute` form values. If the `IDRE-524` feature flag is enabled and the user attempts to add multiple line items while the case type is 'SINGLE', the effect automatically trunc
```
--- components/case-wizard/step-basic-info.tsx
+++ components/case-wizard/step-basic-info.tsx
@@ -1,5 +1,5 @@
 "use client"
 
-import { useState } from "react"
+import { useState, useEffect } from "react"
 import { UseFormReturn } from "react-hook-form"
 import { useFeatureFlagEnabled } from "posthog-js/react"
@@ -36,10 +36,20 @@
   const [showSwitchWarning, setShowSwitchWarning] = useState(false)
   const idre524Enabled = useFeatureFlagEnabled("IDRE-524")
 
+  const typeOfDispute = form.watch("typeOfDispute")
+  const lineItems = form.watch("lineItems")
+
+  useEffect(() => {
+    if (idre524Enabled && typeOfDispute === "SINGLE" && lineItems && lineItems.length > 1) {
+      // Enforce single line item for SINGLE case types
+      form.setValue("lineItems", [lineItems[0]])
+    }
+  }, [idre524Enabled, typeOfDispute, lineItems, form])
+
   const handleConfirmSwitchToSingle = () => {
     const lineItems = form.getValues("lineItems")
     // Keep only the first line item
-    if (lineItems.length > 1) {
+    if (idre524Enabled && lineItems && lineItems.length > 1) {
+      form.setValue("lineItems", [lineItems[0]])
+    } else if (!idre524Enabled && lineItems && lineItems.length > 1) {
       form.setValue("lineItems", [lineItems[0]])
     }
     form.setValue("isBatchedOrBundled", false)
```

## Test Suggestions

Framework: `Jest or Vitest with React Testing Library`

- **shouldTruncateLineItemsToOneWhenTypeIsSingleAndFlagEnabled** — Verifies the bug fix: ensures that when the feature flag is enabled and the case type is SINGLE, the user cannot add more than one line item.
- **shouldAllowMultipleLineItemsWhenFlagIsDisabled** *(edge case)* — Verifies backward compatibility: ensures that if the feature flag is disabled, the truncation logic does not execute.
- **shouldAllowMultipleLineItemsWhenTypeIsNotSingle** — Verifies that the restriction only applies to the 'SINGLE' case type, allowing multiple line items for other case types even when the flag is enabled.

## AI Confidence Scores
Plan: 80%, Code: 85%, Tests: 95%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._