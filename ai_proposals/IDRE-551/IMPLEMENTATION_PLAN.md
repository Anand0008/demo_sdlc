## IDRE-551: When user creates IDRE payouts for current day not able to see in report

**Jira Ticket:** [IDRE-551](https://orchidsoftware.atlassian.net//browse/IDRE-551)

## Summary
This plan addresses a bug where current-day IDRE payouts are not visible in the report. The root cause is a date format mismatch: the frontend sends a full ISO timestamp, while the backend API expects a `YYYY-MM-DD` string, causing the request to be rejected. The fix involves modifying the `components/reports/idre-payouts-report.tsx` component to correctly format the `from` and `to` dates as `YYYY-MM-DD` for both fetching and exporting the report.

## Implementation Plan

**Step 1: Correct Date Format for API Request**  
In the `fetchPayouts` function, locate the `URLSearchParams` initialization around line 93. Modify the `from` and `to` parameters to send dates in the `YYYY-MM-DD` format by changing `validatedFrom.toISOString()` to `validatedFrom.toISOString().split("T")[0]` and `validatedTo.toISOString()` to `validatedTo.toISOString().split("T")[0]`. This will make the client request compliant with the backend API's date format validation schema.
Files: `components/reports/idre-payouts-report.tsx`

**Step 2: Align Date Format for Export Functionality**  
In the `fetchPayouts` function's success block, update the `setAppliedFilters` call around line 102. Ensure the `from` and `to` values stored in the `appliedFilters` state also use the `YYYY-MM-DD` format (e.g., `validatedFrom.toISOString().split("T")[0]`). This maintains date format consistency for the `exportReport` function, which relies on this state.
Files: `components/reports/idre-payouts-report.tsx`

**Risk Level:** LOW — The change is confined to a single client-side component and corrects the date format to match the backend's explicit validation schema. This is a targeted fix with a very low probability of causing unintended side effects. The export functionality is also updated to use the same format, ensuring consistency.

## Proposed Code Changes

### `components/reports/idre-payouts-report.tsx` (modify)
The backend API at `app/api/reports/idre-payouts/route.ts` expects date parameters in `YYYY-MM-DD` format, enforced by a Zod schema. The component was previously sending a full ISO 8601 timestamp (e.g., `2023-10-27T00:00:00.000Z`), causing the API to reject the request with a 400 error.
```
--- a/components/reports/idre-payouts-report.tsx
+++ b/components/reports/idre-payouts-report.tsx
@@ -91,8 +91,8 @@
 
     // Construct query params
     const params = new URLSearchParams({
-      from: validatedFrom.toISOString(),
-      to: validatedTo.toISOString(),
+      from: validatedFrom.toISOString().split("T")[0],
+      to: validatedTo.toISOString().split("T")[0],
     });
 
     const response = await fetch(`/api/reports/idre-payouts?${params.toString()}`);
@@ -103,8 +103,12 @@
   const { data, isLoading, error, refetch } = useQuery({
     queryKey: ['idre-payouts', filters],
     queryFn: fetchPayouts,
-    onSuccess: () => {
-      setAppliedFilters({ from: filters.from, to: filters.to });
+    onSuccess: (data) => {
+      const validatedFrom = filters.from ? new Date(filters.from) : startOfMonth(new Date());
+      const validatedTo = filters.to ? new Date(filters.to) : endOfDay(new Date());
+      setAppliedFilters({
+        from: validatedFrom.toISOString().split("T")[0],
+        to: validatedTo.toISOString().split("T")[0],
+      });
     },
     enabled: false, // Initially disabled, triggered by button click
     staleTime: 1000 * 60 * 5, // 5 minutes
```

**New Dependencies:**
- `None`

## Test Suggestions

Framework: `Vitest`

- **shouldFetchReportWithCorrectDateFormatWhenDateIsSelected** — This is a regression test to verify the primary bug fix. It ensures that when a user selects a date, the component formats it to 'YYYY-MM-DD' before making the API call to fetch the report data, preventing the 400 error.
- **shouldTriggerExportWithCorrectDateFormatInUrl** — This test verifies that the date formatting fix also applies to the export functionality, ensuring consistency and preventing related bugs.

## AI Confidence Scores
Plan: 90%, Code: 90%, Tests: 95%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._