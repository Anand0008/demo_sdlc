## IDRE-607: User is not able to see members under specific organization but able to see under organization reconciliation

**Jira Ticket:** [IDRE-607](https://orchidsoftware.atlassian.net//browse/IDRE-607)

## Summary
Fix member visibility in the specific organization view by ensuring the correct organization ID is passed to the ManageMembersDialog component in both the organization list and management views.

## Implementation Plan

**Step 1: Fix organization ID prop in OrganizationList**  
Update the rendering of the `ManageMembersDialog` component to correctly pass the organization identifier (e.g., `organizationId={selectedOrganization?.id}`) from the `selectedOrganization` state. This ensures the dialog has the necessary context to fetch and display members for the specific organization.
Files: `components/company/organization-list.tsx`

**Step 2: Align ManageMembersDialog props in OrganizationsManagement**  
Update the `ManageMembersDialog` component instantiation in the management/reconciliation view to ensure it passes the exact same prop structure (e.g., `organizationId`) as the specific organization view. This guarantees consistent member visibility and data fetching across both contexts.
Files: `components/organizations-management.tsx`

**Risk Level:** LOW — The changes are isolated to prop passing in client-side React components and do not affect backend logic or database schemas.

## Proposed Code Changes

### `components/company/organization-list.tsx` (modify)
The `ManageMembersDialog` requires the `organizationId` to correctly fetch and display members for the specific organization. Passing it explicitly ensures the dialog has the necessary context.
```
--- components/company/organization-list.tsx
+++ components/company/organization-list.tsx
@@ -100,6 +100,8 @@
       <ManageMembersDialog
         open={manageMembersOpen}
         onOpenChange={setManageMembersOpen}
         organization={selectedOrganization}
+        organizationId={selectedOrganization?.id || ""}
+        organizationName={selectedOrganization?.name || ""}
       />
```

### `components/organizations-management.tsx` (modify)
Aligns the `ManageMembersDialog` props in the management/reconciliation view to ensure it passes the exact same prop structure as the specific organization view, guaranteeing consistent member visibility and data fetching across both contexts.
```
--- components/organizations-management.tsx
+++ components/organizations-management.tsx
@@ -200,6 +200,8 @@
       <ManageMembersDialog
         open={manageMembersOpen}
         onOpenChange={setManageMembersOpen}
         organization={selectedOrganization}
+        organizationId={selectedOrganization?.id || ""}
+        organizationName={selectedOrganization?.name || ""}
       />
```

**New Dependencies:**
- `None`

## Test Suggestions

Framework: `Vitest`

- **should pass the correct organizationId to ManageMembersDialog in OrganizationList** — Verifies that the OrganizationList component correctly passes the organizationId to the ManageMembersDialog, fixing the bug where members were not visible.
- **should pass the correct organizationId to ManageMembersDialog in OrganizationsManagement** — Verifies that the OrganizationsManagement component passes the correct organizationId to the ManageMembersDialog, ensuring consistent behavior with the specific organization view.

## Confluence Documentation References

- [IDRE Dispute Platform Release: Organization Management and Admin Tools Overview](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/315654145) — Provides an overview of the Organization Management and Admin Tools, which is the area where the member visibility bug occurs.
- [Product Requirements Document for IDRE Dispute Platform's Organization Management System](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/302383114) — Contains the product requirements for the Organization Management System, detailing how members should be associated and displayed within organizations.
- [IDRE Platform Weekly Work Summary: April 8, 2026 Updates and Enhancements](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/318275601) — Mentions ongoing work and edge-case resolution for the Organization Management Tool, providing context on the current state of the feature.

**Suggested Documentation Updates:**

- Product Requirements Document for IDRE Dispute Platform's Organization Management System: May need updating if the fix involves changing how members are queried, filtered, or associated with organizations.
- IDRE Dispute Platform Release: Organization Management and Admin Tools Overview: Update if the UI or expected behavior of the organization member view changes as a result of the fix.

## AI Confidence Scores
Plan: 85%, Code: 85%, Tests: 95%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._