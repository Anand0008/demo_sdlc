## IDRE-619: When user clicks on Review and Update button and changes organization for both IP and NIP changes are not getting reflected

**Jira Ticket:** [IDRE-619](https://orchidsoftware.atlassian.net//browse/IDRE-619)

## Summary
This implementation plan addresses a UI bug where organization changes for a party are not reflected after saving. The root cause is identified as a missing cache invalidation step in the server action responsible for the update. The plan involves a single, targeted change: adding a `revalidatePath` call within the appropriate server action in `lib/party-actions.ts`. This will purge the stale data from the server-side cache, ensuring the UI displays the updated organization information upon refresh.

## Implementation Plan

**Step 1: Add cache revalidation to the party organization update action**  
Locate the server action within `lib/party-actions.ts` that is responsible for updating the organization associated with an Initiating Party (IP) or Non-Initiating Party (NIP). This function is called when the "Review and Update" button is clicked. After the database update operation (e.g., `prisma.case.update` or similar) successfully completes, add a call to `revalidatePath` from `next/cache`. The path to revalidate should be the dynamic path for the party details page, for example: `revalidatePath(\`/dashboard/company/party/\${partyUserId}\`)`. This will ensure the page's data cache is purged and fresh data reflecting the organization change is fetched on the next page load.
Files: `lib/party-actions.ts`

**Risk Level:** LOW — The change is low-risk as it only involves adding a cache revalidation call to an existing server action. This is a standard Next.js pattern for ensuring data freshness and is unlikely to have side effects. The change is confined to a single server-side file.

## Proposed Code Changes

### `lib/party-actions.ts` (modify)
The ticket describes an issue where UI data becomes stale after updating a party's organization. This is a classic caching problem in Next.js. By adding `revalidatePath`, we are explicitly telling Next.js to purge the cached data for the specified party's detail page. This ensures that the next time the page is visited, it will fetch fresh data from the database, reflecting the organization change and resolving the bug.
```typescript
--- a/lib/party-actions.ts
+++ b/lib/party-actions.ts
@@ -1,5 +1,6 @@
 'use server';
 
+import { revalidatePath } from 'next/cache';
 import { z } from 'zod';
 
 import { prisma } from '@/lib/database';
@@ -33,6 +34,8 @@
           : { nonInitiatingPartyOrganizationId: organizationId },
     });
 
+    revalidatePath(`/dashboard/company/party/${partyUserId}`);
+
     return { success: 'Organization updated successfully.' };
   } catch (error) {
     console.error('Failed to update party organization:', error);
```

**New Dependencies:**
- `None`

## Test Suggestions

Framework: `Jest / Vitest`

- **shouldCallRevalidatePathOnSuccessfulUpdate** — This test verifies the core logic of the fix. It ensures that after a successful database update, the cache invalidation function (`revalidatePath`) is called to prevent the UI from showing stale data.
- **shouldNotCallRevalidatePathOnFailedUpdate** *(edge case)* — This test ensures that if the database update fails, we do not proceed to invalidate the cache. This prevents a state where the cache is cleared for a page whose underlying data has not actually changed, which could lead to unnecessary database reads or inconsistent states.

## Confluence Documentation References

- [Product Requirements Document for IDRE Dispute Platform's Organization Management System](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/302383114) — This PRD is the primary source of truth for the Organization Management feature. It details the expected behavior, user flows, and business rules that are essential for a developer to understand before fixing a bug related to updating an organization.
- [IDRE Dispute Platform Release: Organization Management and Admin Tools Overview](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/315654145) — This release overview provides context for the Organization Management feature, including its purpose and user interface. It helps confirm the scope of the tool mentioned in the ticket.

**Suggested Documentation Updates:**

- Product Requirements Document for IDRE Dispute Platform's Organization Management System: This PRD should be reviewed and updated after the fix is implemented to ensure the documented workflow and business rules precisely match the corrected behavior of the 'Review and Update' functionality.

## AI Confidence Scores
Plan: 90%, Code: 90%, Tests: 95%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._