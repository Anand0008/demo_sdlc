## IDRE-648: Update dispute # to show dispute ID, not unique identifier

**Jira Ticket:** [IDRE-648](https://orchidsoftware.atlassian.net//browse/IDRE-648)

## Summary
Update the Organization Reconciliation (Payment Variance) Mismatches view to correctly map and format the Dispute # column using the human-readable dispute ID with a `DISP-` prefix.

## Implementation Plan

**Step 1: Update Dispute # column mapping and formatting in Payment Variance report**  
Locate the table or data grid rendering the Mismatches view within the PaymentVarianceReportPage. Update the 'Dispute #' column definition to map to the human-readable dispute number field (e.g., `disputeNumber` or `caseNumber`) instead of the internal 7-character alphanumeric identifier. Ensure the cell rendering logic formats the value with the `DISP-` prefix (e.g., `DISP-${disputeNumber}`). If the page is a Server Component fetching its own data, ensure the query selects the correct dispute number field.
Files: `app/dashboard/reports/payment-variance/page.tsx`

**Risk Level:** LOW — The change is isolated to the presentation layer of a specific report (Payment Variance / Org Reconciliation) and only affects how the dispute number is displayed. It does not alter underlying business logic or data structures.

## Proposed Code Changes

### `app/dashboard/reports/payment-variance/page.tsx` (modify)
The "Dispute #" column was previously mapping to the internal unique identifier (e.g., `QRHYHFR`). This change updates the column definition to map to the human-readable dispute number (`disputeNumber` or `caseNumber`) and ensures it is consistently formatted with the `DISP-` prefix, satisfying the acceptance criteria.
```
--- a/app/dashboard/reports/payment-variance/page.tsx
+++ b/app/dashboard/reports/payment-variance/page.tsx
@@ -50,8 +50,14 @@
   {
-    accessorKey: "disputeId",
+    accessorKey: "disputeNumber",
     header: "Dispute #",
+    cell: ({ row }) => {
+      // Map to the human-readable dispute number, falling back to disputeId if necessary
+      const disputeVal = row.original?.disputeNumber || row.original?.caseNumber || row.original?.disputeId || row.getValue("disputeNumber");
+      if (!disputeVal) return null;
+      
+      const formatted = String(disputeVal).startsWith("DISP-") ? disputeVal : `DISP-${disputeVal}`;
+      return <span>{formatted}</span>;
+    },
   },
```

**New Dependencies:**
- `None`

## Test Suggestions

Framework: `Vitest / React Testing Library`

- **should render Dispute # column with DISP- prefix using the human-readable dispute number** — Verifies that the bug is fixed by ensuring the human-readable dispute number is used and formatted with the DISP- prefix, rather than the internal unique identifier.
- **should not duplicate the DISP- prefix if the dispute number already includes it** *(edge case)* — Ensures that the formatting logic is idempotent and doesn't double-prefix dispute numbers that already have the correct format.
- **should render fallback text when dispute number is missing** *(edge case)* — Verifies graceful fallback when the dispute number data is missing from the backend.

## Confluence Documentation References

- [IDRE Worflow](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/284688394) — This page explicitly defines the business rule and standard format for dispute reference numbers (DISP-XXXXXXX), which is the target format required by the ticket.

**Suggested Documentation Updates:**

- Product Requirements Document for IDRE Dispute Platform's Organization Management System
- IDRE Dispute Platform Release: Organization Management and Admin Tools Overview

## AI Confidence Scores
Plan: 85%, Code: 85%, Tests: 95%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._