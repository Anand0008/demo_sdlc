## IDRE-673: Track and report on why individual disputes are found ineligible

**Jira Ticket:** [IDRE-673](https://orchidsoftware.atlassian.net//browse/IDRE-673)

## Summary
Extend the 'Ineligible' case status by adding an 'ineligibilityReason' field to the database. Update the eligibility workflow to prompt users for this reason when marking a case ineligible, and expose these sub-statuses as filters on the cases dashboard.

## Implementation Plan

**Step 1: Update Database Schema**  
Add an `IneligibilityReason` enum containing the 13 specified reasons (e.g., `ELIGIBLE_FOR_STATE_PROCESS`, `COOLING_OFF_PERIOD_NOT_COMPLETED`, etc.). Add an `ineligibilityReason` field of type `IneligibilityReason?` to the `Case` model.
Files: `prisma/schema.prisma`

**Step 2: Prompt for Ineligibility Reason in Workflow**  
Update the action/modal where a user marks a dispute as 'Ineligible' to include a required dropdown for selecting the `ineligibilityReason`. Ensure this value is passed to the backend mutation (e.g., `updateCaseStatus`).
Files: `components/shared/workflow-modules/eligibility-workflow.tsx`

**Step 3: Update Dashboard Filters and Display**  
Add the new ineligible sub-statuses to the status filter dropdown, formatted as 'Ineligible – [Reason]'. Update the filtering logic to correctly filter cases by `ineligibilityReason` when an ineligible sub-status is selected.
Files: `app/app/cases/components/cases-page-client.tsx`

**Step 4: Update Search API for Sub-statuses**  
Update the search and filter API logic to support querying by `ineligibilityReason` when an ineligible sub-status filter is applied from the frontend.
Files: `app/api/cases/search/route.ts`

**Step 5: Add Enum Helper Tests**  
Add unit tests for formatting the new `IneligibilityReason` enum values into the required 'Ineligible – [Reason]' display format, ensuring all 13 reasons are covered.
Files: `tests/utils/enum-helpers.test.ts`

**Risk Level:** MEDIUM — Modifying case closure statuses is a known complexity hotspot that can impact payment and refund triggers. By adding a separate 'ineligibilityReason' field rather than changing the core 'INELIGIBLE' status enum, we mitigate the risk to financial workflows, but thorough regression testing is still required.
⚠️ **Database Migrations Required: YES**

**Deployment Notes:**
- A Prisma migration must be generated and applied to add the new IneligibilityReason enum and the ineligibilityReason field to the Case table.

## Proposed Code Changes

### `prisma/schema.prisma` (modify)
No rationale provided
```
Type: modify

```diff
--- a/prisma/schema.prisma
+++ b/prisma/schema.prisma
@@ -100,6 +100,7 @@
   status                            CaseStatus                 @default(OPEN)
+  ineligibilityReason               IneligibilityReason?
   createdAt                         DateTime                   @default(now())
@@ -1000,6 +1001,22 @@
+enum IneligibilityReason {
+  ELIGIBLE_FOR_STATE_PROCESS
+  EXCEEDED_FOUR_DAY_TIMELINE
+  INCORRECTLY_BATCHED
+  INCORRECTLY_BUNDLED
+  ITEM_OR_SERVICE_NOT_COVERED_BY_PLAN
+  ITEM_OR_SERVICE_NOT_NSA_ELIGIBLE
+  NOTICE_OF_INITIATION_NOT_SUBMITTED
+  OPEN_NEGOTIATION_NOT_COMPLETE
+  OPEN_NEGOTIATION_NOT_INITIATED
+  OTHER
+  PLAN_NOT_SUBJECT_TO_NSA
+  PRIOR
```

## Test Suggestions

Framework: `Jest / React Testing Library`

- **shouldPromptForIneligibilityReasonWhenStatusIsIneligible** — Verifies that the user is prompted to select a sub-status when marking a dispute as Ineligible
- **shouldRequireIneligibilityReasonBeforeSubmission** *(edge case)* — Ensures that an ineligibility reason is strictly required when the Ineligible status is selected
- **shouldFilterCasesByIneligibilityReason** — Verifies that the dashboard correctly filters cases by the new ineligibility sub-statuses
- **shouldSaveIneligibilityReasonToDatabase** — Verifies that the backend API correctly accepts and saves the new ineligibilityReason field to the database

## Confluence Documentation References

- [IDRE Worflow](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/284688394) — This is the canonical reference for the case lifecycle and statuses, which will be directly impacted by the introduction of new Ineligible sub-statuses.
- [Bugs](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/285736962) — Identifies 'Ineligible' as a closure type that interacts heavily with the complex payments and refunds engine, highlighting a critical area for regression testing when modifying this status.

**Suggested Documentation Updates:**

- IDRE Worflow: Needs to be updated to reflect the new 'Ineligible - [Reason]' sub-statuses in the case lifecycle and eligibility review phase.

## AI Confidence Scores
Plan: 90%, Code: 90%, Tests: 90%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._