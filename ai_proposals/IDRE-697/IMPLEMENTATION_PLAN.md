## IDRE-697: When trying to add more than one bank account to main org bank account which was added previously is getting removed

**Jira Ticket:** [IDRE-697](https://orchidsoftware.atlassian.net//browse/IDRE-697)

## Summary
This plan addresses a UI bug where adding a new bank account causes the existing one to disappear. The root cause is likely an incorrect `key` prop in the React component list rendering on the banking page. The fix involves updating the `.map()` function in `app/app/banking/page.tsx` to use a unique and stable ID for each `BankAccountCard` component's `key`, ensuring the UI updates correctly when the list of accounts changes.

## Implementation Plan

**Step 1: Correct the `key` prop in the bank account list rendering**  
In `app/app/banking/page.tsx`, locate the code that maps over the array of bank accounts to render the list. Inside this loop, the `<BankAccountCard>` component is rendered. Ensure that the `key` prop for this component is set to a unique and stable identifier from the bank account object, such as `bankAccount.id`. This will prevent React from misidentifying components when a new account is added to the list.
Files: `app/app/banking/page.tsx`

**Risk Level:** LOW — The proposed change is a minor, targeted fix to a common React rendering pattern. It is isolated to a single component and has a very low risk of causing unintended side effects. The fix directly addresses the symptoms described in the bug report.

## Proposed Code Changes

### `app/app/banking/page.tsx` (modify)
The implementation plan identified that the list of bank account cards was likely using the array index as the `key` prop. This is an anti-pattern in React that can lead to incorrect UI state and rendering issues when the list is modified. By changing the key from `index` to the unique and stable `bankAccount.id`, we ensure that React can correctly track each bank account component, preventing them from being incorrectly removed from the DOM when a new account is added.
```
--- a/app/app/banking/page.tsx
+++ b/app/app/banking/page.tsx
@@ -248,9 +248,9 @@
                 Add Bank Account
               </Button>
             )}
-            {ownBankAccounts.map((bankAccount, index) => (
+            {ownBankAccounts.map((bankAccount) => (
               <BankAccountCard
-                key={index}
+                key={bankAccount.id}
                 bankAccount={bankAccount}
                 organizationId={selectedOrganization.id}
                 isDefault={
```

**New Dependencies:**
- `None`

## Test Suggestions

Framework: `Jest/Vitest with @testing-library/react`

- **shouldKeepExistingBankAccountsVisibleWhenANewOneIsAdded** — This is a regression test to confirm that adding a new bank account does not cause previously rendered accounts to disappear from the UI. It directly validates the fix of using a stable `key` prop (e.g., `bankAccount.id`) instead of the array index.

## Confluence Documentation References

- [Product Requirements Document for IDRE Dispute Platform's Organization Management System](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/302383114) — This is the Product Requirements Document (PRD) for the feature described in the ticket. It should contain the definitive business rules regarding how an organization's bank accounts are managed, including the rule that an organization can have more than one account.

**Suggested Documentation Updates:**

- Product Requirements Document for IDRE Dispute Platform's Organization Management System

## AI Confidence Scores
Plan: 90%, Code: 85%, Tests: 95%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._