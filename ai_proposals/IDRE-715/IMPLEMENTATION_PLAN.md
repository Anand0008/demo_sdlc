## IDRE-715: Duplicate Orgzanization Tool -  Moldal exceeds  the size of page

**Jira Ticket:** [IDRE-715](https://orchidsoftware.atlassian.net//browse/IDRE-715)

## Summary
This plan addresses the UI bug where the Duplicate Organization tool's merge modal exceeds the page size. The fix involves a targeted CSS change to the `MergeOrganizationModal` component, making its content area scrollable by applying a maximum height and enabling vertical overflow. This will ensure the modal fits within the viewport regardless of the number of duplicate organizations listed.

## Implementation Plan

**Step 1: Apply max-height and overflow styles to the modal content area**  
In the `MergeOrganizationModal` component, locate the primary content container element that wraps the list of duplicate organizations. This is the element that is currently overflowing. Apply CSS classes to this container to constrain its height and enable vertical scrolling. Using Tailwind CSS, add the classes `max-h-[75vh]` and `overflow-y-auto`. This will limit the container's maximum height to 75% of the viewport height and add a scrollbar only when the content exceeds that height, resolving the overflow issue.
Files: `app/dashboard/admin-tools/duplicate-orgs/components/merge-organization-modal.tsx`

**Risk Level:** LOW — The change is a low-risk CSS modification scoped to a single modal component within an admin-only tool. It has no impact on backend logic, data, or user-facing features. The fix is standard practice for handling overflowing content in modals.

## Proposed Code Changes

### `components/case-review/organization-assignments-edit-modal.tsx` (modify)
As per the implementation plan, the modal content area was overflowing when the list of duplicate organizations was long. This change applies a maximum height (75% of the viewport height) and enables vertical scrolling on the main content `div` within the modal. This ensures that all content remains accessible within the modal without breaking the page layout.
```
--- a/app/dashboard/admin-tools/duplicate-orgs/components/merge-organization-modal.tsx
+++ b/app/dashboard/admin-tools/duplicate-orgs/components/merge-organization-modal.tsx
@@ -49,7 +49,7 @@
             Select one organization to keep and merge the others into it. All data from the merged
             organizations will be transferred to the selected organization.
           </ModalDescription>
         </ModalHeader>
-        <div className="p-6 grid gap-6">
+        <div className="p-6 grid gap-6 max-h-[75vh] overflow-y-auto">
           <div className="grid grid-cols-2 gap-4">
             <div className="space-y-2">
               <h3 className="font-semibold">Organizations to Merge</h3>
```

**New Dependencies:**
- `None`

## Test Suggestions

Framework: `Vitest`

- **shouldBeScrollableWhenListOfOrganizationsIsLong** *(edge case)* — This test directly verifies the bug fix. It ensures that when the number of organizations would typically cause the modal to overflow, the component applies the correct CSS to contain the content within the viewport and make it scrollable.
- **shouldRenderCorrectlyWithoutScrollbarForShortList** — This is a happy path test to ensure that the fix for long lists does not negatively impact the UI for short lists where scrolling is unnecessary.

## AI Confidence Scores
Plan: 80%, Code: 85%, Tests: 95%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._