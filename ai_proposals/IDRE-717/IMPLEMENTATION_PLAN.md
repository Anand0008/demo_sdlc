## IDRE-717: Duplicate Organization Detection -  Members and Invoices not loading

**Jira Ticket:** [IDRE-717](https://orchidsoftware.atlassian.net//browse/IDRE-717)

## Summary
This plan resolves the issue of members and invoices not loading on the Duplicate Organization Detection page. The root cause is identified as the backend API not providing the necessary data. The fix involves updating the `app/api/organizations/search/route.ts` endpoint to include associated members and invoices in the payload when an organization's details are requested by ID.

## Implementation Plan

**Step 1: Update Organization Search API to Include Members and Invoices**  
Modify the GET handler in `app/api/organizations/search/route.ts`. The file summary indicates it can already search by ID. Enhance the existing Prisma query for fetching an organization by ID to also `include` its related members (likely via the `users` relation) and its `invoices`. This will ensure that when the front-end requests details for a specific organization, the API response contains the necessary data for the modals to display.
Files: `app/api/organizations/search/route.ts`

**Risk Level:** LOW — The change is confined to a single API endpoint and only adds data to an existing response for a specific query type (fetching by ID). It is unlikely to impact the primary search functionality of the endpoint or other parts of the application.

## Proposed Code Changes

### `app/api/organizations/search/route.ts` (modify)
According to the implementation plan, the frontend modals for displaying an organization's members and invoices were not loading because the API was not providing this data. This change modifies the Prisma query for fetching a single organization by ID to `include` the `users` (members) and `invoices` relations. The API response will now contain all the data required by the UI, resolving the issue.
```typescript
--- a/app/api/organizations/search/route.ts
+++ b/app/api/organizations/search/route.ts
@@ -21,7 +21,11 @@
       if (organizationId) {
         const organization = await db.organization.findUnique({
           where: { id: organizationId },
+          include: {
+            users: true, // For members list
+            invoices: true, // For invoices list
+          },
         });
 
         if (!organization) {
```

**New Dependencies:**
- `None`

## Confluence Documentation References

- [Product Requirements Document for IDRE Dispute Platform's Organization Management System](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/302383114) — This is the Product Requirements Document (PRD) for the feature area in which the bug occurs. It is the most likely source for business rules and expected behavior regarding how duplicate organizations, members, and invoices should be handled.
- [IDRE Dispute Platform Release: Organization Management and Admin Tools Overview](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/315654145) — This document provides a high-level overview of the Organization Management feature. It can offer context on the intended functionality and how the duplicate detection feature fits into the overall admin toolset.
- [IDRE Platform Weekly Work Summary: April 8, 2026 Updates and Enhancements](https://orchidsoftware.atlassian.net/wiki/spaces/IDRE/pages/318275601) — This weekly summary provides important context that the Organization Management tool is a new feature where the team is actively addressing "edge-case issues." This ticket, concerning a bug with duplicate organizations, falls directly into that category.

**Suggested Documentation Updates:**

- Product Requirements Document for IDRE Dispute Platform's Organization Management System

## AI Confidence Scores
Plan: 80%, Code: 95%

---
> ⚠️ **This PR was generated by AI (Claude via AWS Bedrock) and requires thorough human review
> before merging. Verify all logic, test coverage, and edge cases independently.**
>
> _Generated by [Artoo](https://github.com/Telomere-techsupp/SDLCWorker) — by Telomere LLC_
> _© 2025-2026 Telomere LLC. All rights reserved._