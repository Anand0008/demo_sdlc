# API Design Standards

This module implements authentication and API security patterns
as defined in the Telomere API Design Standards Confluence page.

## Key Features
- Account lockout after N consecutive failed logins (IN-1)
- Configurable session expiry (IN-2)
- Security headers on all responses (IN-8)
